# Frontend env template — copy to .env (gitignored).
# Vite exposes VITE_* vars to the browser at build time.

# Sentry — leave empty to disable (init becomes a no-op).
# In Railway: set on the frontend service.
VITE_SENTRY_DSN=
